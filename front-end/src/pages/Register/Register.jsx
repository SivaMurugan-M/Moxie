import React,{useContext,useState}from"react";import{Link,useNavigate}from"react-router-dom";import{AuthContext}from"../../context/AuthContext";import"../Login/Login.css";export default function Register(){const{register}=useContext(AuthContext);const navigate=useNavigate();const[show,setShow]=useState(false);const[error,setError]=useState("");const submit=e=>{e.preventDefault();const d=new FormData(e.currentTarget),name=d.get("name").trim(),email=d.get("email").trim(),password=d.get("password");if(name.length<2)return setError("Please enter your full name.");if(!/^\S+@\S+\.\S+$/.test(email))return setError("Please enter a valid email.");if(password.length<6)return setError("Password must be at least 6 characters.");register(name,email);navigate("/")};return <main className="auth-page"><section className="auth-panel"><Link to="/" className="auth-brand">MOXIE</Link><span className="eyebrow">Join the club</span><h1>Create your account</h1><p>Save favourites and enjoy a quicker mock checkout.</p>{error&&<div className="auth-error">{error}</div>}<form onSubmit={submit}><label>Full name<input name="name"/></label><label>Email address<input name="email" type="email"/></label><label>Password<div className="password-field"><input name="password" type={show?"text":"password"}/><button type="button" onClick={()=>setShow(!show)}>{show?"Hide":"Show"}</button></div></label><button className="primary-btn auth-submit">Create account</button></form><p className="auth-switch">Already registered? <Link to="/login">Sign in</Link></p></section><aside className="auth-aside"><span>JOIN MOXIE</span><h2>Your collection, all in one place.</h2></aside></main>}
