import React,{useContext,useState}from"react";import{Link,useNavigate}from"react-router-dom";import{AuthContext}from"../../context/AuthContext";import"./Login.css";export default function Login(){const{login}=useContext(AuthContext);const navigate=useNavigate();const[show,setShow]=useState(false);const[message,setMessage]=useState("");const submit=e=>{e.preventDefault();const d=new FormData(e.currentTarget),email=d.get("email").trim(),password=d.get("password");if(!/^\S+@\S+\.\S+$/.test(email))return setMessage("Please enter a valid email address.");if(password.length<6)return setMessage("Password must be at least 6 characters.");login(email);navigate("/")};return <main className="auth-page"><section className="auth-panel"><Link to="/" className="auth-brand">MOXIE</Link><span className="eyebrow">Welcome back</span><h1>Sign in to Moxie</h1><p>Access your saved wishlist and keep shopping.</p>{message&&<div className="auth-error">{message}</div>}<form onSubmit={submit}><label>Email address<input name="email" type="email" placeholder="you@example.com"/></label><label>Password<div className="password-field"><input name="password" type={show?"text":"password"} placeholder="Your password"/><button type="button" onClick={()=>setShow(!show)}>{show?"Hide":"Show"}</button></div></label><div className="auth-options"><label><input type="checkbox"/> Remember me</label><button type="button" onClick={()=>setMessage("Password reset is a frontend demo.")}>Forgot password?</button></div><button className="primary-btn auth-submit">Sign in</button></form><p className="auth-switch">New to Moxie? <Link to="/register">Create an account</Link></p></section><aside className="auth-aside"><span>YOUR STYLE. YOUR SPACE.</span><h2>Save favourites and pick up right where you left off.</h2></aside></main>}
