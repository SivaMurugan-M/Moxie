import React,{useContext}from"react";import{Link}from"react-router-dom";import{CartContext}from"../../context/CartContext";import{WishlistContext}from"../../context/WishlistContext";import{useToast}from"../../context/ToastContext";import"./ProductCard.css";export default function ProductCard({product}){const{cart,addToCart}=useContext(CartContext);const{toggleWishlist,isInWishlist}=useContext(WishlistContext);const toast=useToast(),inCart=cart.some(item=>item.id===product.id),wished=isInWishlist(product.id);const wishlist=e=>{e.preventDefault();e.stopPropagation();toggleWishlist(product);toast(wished?"Removed from wishlist":"Saved to wishlist")};const add=e=>{e.preventDefault();e.stopPropagation();if(!product.stock)return;addToCart(product);toast(`${product.name} added to cart`)};return <article className="product-card"><Link to={`/products/${product.id}`} className="product-card-link"><div className="product-card-media">{product.discount>0&&<span className="discount-badge">{product.discount}% OFF</span>}{product.isNew&&<span className="new-badge">NEW</span>}<button className={`card-heart ${wished?"active":""}`} onClick={wishlist}>{wished?"♥":"♡"}</button><img src={product.image} alt={product.name}/></div><div className="product-card-body"><span className="product-category">{product.category}</span><h3>{product.name}</h3><div className="rating-row"><span className="rating-pill">★ {product.rating}</span><span>({product.reviewCount})</span></div><div className="price-row"><strong>₹{product.price.toLocaleString("en-IN")}</strong><del>₹{product.oldPrice.toLocaleString("en-IN")}</del></div><span className={`stock-status ${product.stock?"available":"unavailable"}`}>{product.stock?"In stock":"Out of stock"}</span></div></Link><button className={`add-cart-button ${inCart?"added":""}`} disabled={!product.stock} onClick={add}>{!product.stock?"Unavailable":inCart?"Add another":"Add to cart"}</button></article>}
