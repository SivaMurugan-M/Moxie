import React,{useState}from"react";import{useNavigate}from"react-router-dom";import"./SearchBar.css";import Search from"../../../assets/icons/search.svg";import products from"../../../data/products";export default function SearchBar({searchQuery,setSearchQuery}){const[isOpen,setIsOpen]=useState(false),navigate=useNavigate(),query=(searchQuery||"").toLowerCase().trim(),suggestions=query?products.filter(p=>p.name.toLowerCase().includes(query)||p.category.toLowerCase().includes(query)).slice(0,6):[];const select=p=>{setSearchQuery("");setIsOpen(false);navigate(`/products/${p.id}`)};const submit=e=>{e.preventDefault();if(query)navigate(`/products?search=${encodeURIComponent(query)}`);setIsOpen(false)};return <form className="search-wrapper" onSubmit={submit}><div className="search-container"><input id="search-bar" value={searchQuery} onChange={e=>{setSearchQuery(e.target.value);setIsOpen(true)}} onFocus={()=>setIsOpen(true)} onBlur={()=>setTimeout(()=>setIsOpen(false),200)} placeholder="Search gadgets, lifestyle, tech..."/>{searchQuery&&<button type="button" className="search-clear" onClick={()=>setSearchQuery("")}>×</button>}<button id="search-icon" type="submit"><img src={Search} alt="Search"/></button></div>{isOpen&&query&&<div className="search-suggestions-dropdown">{suggestions.map(p=><div key={p.id} className="suggestion-item d-flex align-items-center" onMouseDown={e=>e.preventDefault()} onClick={()=>select(p)}><img src={p.image} alt={p.name} className="suggestion-image"/><div className="suggestion-details ms-3"><h5 className="suggestion-title mb-0">{p.name}</h5><span className="suggestion-category">{p.category}</span></div><span className="suggestion-price ms-auto">₹{p.price.toLocaleString("en-IN")}</span></div>)}{!suggestions.length&&<div className="no-suggestions text-center py-3">No products found</div>}</div>}</form>}
